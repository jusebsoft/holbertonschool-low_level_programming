#include "main.h"

/**
* _abs - function to look the absolute value of a number
* @a: function parameter
* Description: se usa dos condicionales para obtener el valor absolut
* Return: -a or a.
*/

int _abs(int a)
{
	if (a < 0)
		return (-a);
	else if
	{
		return (a);
	}
	return (0);
}
